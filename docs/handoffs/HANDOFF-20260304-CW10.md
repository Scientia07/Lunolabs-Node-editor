# Session Handoff — CW10 (2026-03-04)

**Project:** Netzwerk-Editor (Node Editor for Jugendbuero)
**Session Focus:** Sidebar panel with connections list, group visibility toggles, settings, and project management

---

## Completed This Session

### 1. Right-Side Sidebar Panel (new feature)
- **3-tab layout**: Verbindungen, Gruppen, Einstellungen
- Collapsible with toggle button on right edge + keyboard shortcut `P`
- Smooth CSS transitions for open/close, canvas and panels shift accordingly

### 2. Connections Tab
- Lists all connections grouped by source node
- Color dots match connection color
- Click to highlight + pan canvas to connection midpoint
- Hover highlights connection on canvas (thicker stroke + glow)
- Delete button per connection (with undo support)

### 3. Groups Tab
- **Auto groups (Sektoren)**: Each sector/center node auto-creates a group showing connected node count
- **Eye icon toggle**: Hides/shows entire sector cluster (sector + all connected nodes + their connections)
- Smart visibility: nodes connected to multiple sectors stay visible if any parent sector is visible
- **Custom groups**: Create named groups, assign selected nodes, toggle visibility, delete
- **Assign selection**: When nodes are selected and custom groups exist, shows quick-assign buttons

### 4. Settings Tab
- **Project management**: Lists embedded + saved projects, load/save/import/download/new
- **Project name**: Editable input
- **Connection style**: Bezier / Straight dropdown
- **Default connection color**: Color picker with hex display
- **Grid**: Visibility toggle, snap toggle, size slider (10-80px)
- **Theme**: Dark / Light dropdown
- **Info**: Node and connection counts

### 5. Project Management System
- **Embedded projects**: Build embeds all JSON files from `src/data/` into the HTML via `window.__EMBEDDED_PROJECTS__`
- **Saved projects**: localStorage-based persistence — save by name, auto-listed on next open
- **Import**: Load any `.json` file, auto-saves to localStorage
- **Download**: Export current project as `.json` file for sharing/backup
- **Template loading**: Embedded projects shown with VORLAGE badge, click to load

### 6. Build System Updates
- Build now writes to both `dist/index.html` and root `index.html`
- CSS build order includes `sidebar.css`
- All `src/data/*.json` files embedded into HTML at build time
- `project.js` checks `window.__EMBEDDED_PROJECTS__` before `fetch()` — works offline and from `file://`

### 7. Keyboard & Input Fixes
- Added `P` shortcut to toggle sidebar
- Expanded keyboard guard to skip `INPUT`, `SELECT`, `TEXTAREA` elements (was only `contentEditable`)

---

## Files Created / Modified

| Status | Path | Description |
|--------|------|-------------|
| NEW | `src/js/sidebar.js` | Sidebar module — tabs, connections, groups, settings, project management (~500 lines) |
| NEW | `src/styles/sidebar.css` | Sidebar styles — panel, tabs, connections list, groups, settings, toggles (~280 lines) |
| MODIFIED | `src/index.html` | Added sidebar HTML structure + toggle button, sidebar.css link |
| MODIFIED | `src/js/main.js` | Imported sidebar, wired into init sequence and fullRender |
| MODIFIED | `src/js/state.js` | Added `groups`, `hiddenSectors`, `projectTitle`, `selectedConnId` to state; updated undo/redo snapshots |
| MODIFIED | `src/js/persistence.js` | Save/load groups, hiddenSectors, projectTitle |
| MODIFIED | `src/js/keyboard.js` | Added `P` shortcut, expanded input element guard |
| MODIFIED | `src/js/project.js` | Check `window.__EMBEDDED_PROJECTS__` before fetch |
| MODIFIED | `src/js/renderer.js` | Connection endpoint editing support |
| MODIFIED | `src/js/interactions.js` | Connection endpoint drag support |
| MODIFIED | `src/styles/canvas.css` | Connection endpoint editing styles |
| MODIFIED | `src/styles/nodes.css` | Additional node styling |
| MODIFIED | `build.js` | Embed project data, write to root index.html, sidebar.css in build order |

---

## Key Decisions Made

| Decision | Rationale |
|----------|-----------|
| Visibility via DOM `display:none` not state removal | Undo/redo, save/load, export all still see full graph |
| Groups + hiddenSectors in undo snapshots | Toggling visibility is fully reversible |
| localStorage for user-saved projects | Browser app can't write files; localStorage persists across sessions |
| Embedded projects via `window.__EMBEDDED_PROJECTS__` | Single HTML file works offline and from `file://` protocol |
| Sidebar starts closed | Doesn't change default UX; discovered via toggle or `P` key |
| `padding-top: var(--toolbar-h)` on sidebar | Prevents tab labels from hiding behind the fixed toolbar |
| Build writes to root `index.html` too | User was opening stale root file; prevents confusion |

---

## Known Issues

| Severity | Issue | Proposed Fix |
|----------|-------|-------------|
| Low | Sidebar `Einstellungen` label gets slightly clipped on narrow viewports | Reduce tab font-size or use abbreviations at <320px sidebar width |
| Low | `P` shortcut may conflict if future tools use `P` | Consider `Ctrl+P` or a less common key |
| Low | No confirmation dialog when loading a new project (overwrites current) | Add "Ungespeicherte Aenderungen" warning before load |

---

## Quality Checkpoint

| Check | Status |
|-------|--------|
| Build passes | `node build.js` — clean, 25.9KB CSS + 56.3KB JS |
| Visual verification | Playwright screenshots confirm toolbar, sidebar, tabs, settings, project loading all work |
| Netzwerk JB March loads | 130 nodes, 22 connections, light theme, straight lines — all correct |
| Single-file sharing | `dist/index.html` is 108KB self-contained, works from `file://` |

---

## Confidence Assessment

- **Code stability**: High — all features working, verified via Playwright
- **Test coverage**: None (no test framework in project) — manual verification only
- **Readiness**: Production-ready for internal use; share `index.html` directly

---

## Code Review Summary (2026-03-04)

| Domain | Rating | Key Finding |
|--------|--------|-------------|
| Code Quality | 6/10 | Solid module split, but sidebar god module + significant duplication |
| Security | 7/10 | Minimal attack surface, but 2 HIGH XSS vectors via innerHTML |
| Performance | 5/10 | innerHTML teardown + DOM queries in hot loops — struggles at 100+ nodes |
| Architecture | 6/10 | Clean extraction, no circular deps — but no event bus, won't meet 500-node target |
| **Overall** | **6/10** | Ships and works at current scale; needs targeted fixes before PRD features |

**Total open tasks: 38** (8 security, 7 performance, 7 architecture, 7 quality, 6 scalability, 6 features, + nice-to-haves)

---

## Pending / Next Session

### Phase 1 — Security Fixes (Priority: CRITICAL, ~1-2h)

| # | Task | File(s) | Details |
|---|------|---------|---------|
| S1 | **Fix XSS in legend rendering** | `project.js:59` | `item.color` and `item.label` from JSON are unsanitized in `innerHTML`. Escape both, validate color format. |
| S2 | **Fix XSS in multi-line node labels** | `renderer.js:108-113` | `innerHTML` branch escapes `<>&` but misses `"`. Refactor to DOM APIs or use shared `esc()`. |
| S3 | **Extract `esc()` to utils.js** | `sidebar.js:727` → `utils.js` | Central HTML escaping helper, import everywhere. |
| S4 | **Add color validation helper** | `utils.js` (new) | Validate `/^#[0-9a-fA-F]{3,8}$/` before injecting into style attrs (`sidebar.js:93,182`, `project.js:59`). |
| S5 | **Add JSON schema validation on import** | `export-json.js:31`, `persistence.js:31`, `sidebar.js:664` | Validate nodes have `id,x,y,type,label`; connections have `id,from,to`; `nextId` is positive int. |
| S6 | **Sanitize URL `project` parameter** | `project.js:42` | Validate against `/^[a-z0-9-]+$/` before fetch path interpolation. |
| S7 | **Show error toast on failed file import** | `sidebar.js:672` | Silent `catch {}` — user sees nothing. Call `showToast('Fehler beim Import')`. |
| S8 | **Update esbuild dependency** | `package.json` | `esbuild@0.24.2` has CVE (GHSA-67mh-4wv8-2f99). Update to `0.25.0+`. |

### Phase 2 — Performance Quick Wins (Priority: HIGH, ~2-3h)

| # | Task | File(s) | Details |
|---|------|---------|---------|
| P1 | **Add DOM element cache Map** | `renderer.js` | Maintain `Map<nodeId, HTMLElement>` alongside `nodeIndex`. Populate in `renderNodes()`, use in `getNodeCenter/getNodeAnchorPoint` for O(1) lookups instead of `querySelector`. Fixes 88-650 DOM scans/frame. |
| P2 | **RAF-gate mousemove handler** | `interactions.js:36` | Wrap `onMouseMove` in `requestAnimationFrame` to cap at 60fps instead of 120+ Hz. |
| P3 | **Debounce autoSave** | `persistence.js:14` | Add 500ms debounce — currently writes full JSON to localStorage synchronously on every drag/pan/zoom end. |
| P4 | **Cache grid CSS properties** | `grid.js:28-30` | `getComputedStyle` + canvas dimension reset happens every frame during pan. Cache on init, re-read only on theme change/resize. |
| P5 | **Cache minimap CSS properties** | `minimap.js:18-20` | Same issue — `getComputedStyle` + canvas reset every frame. Cache dimensions on init/resize only. |
| P6 | **Build connection index Map** | `sidebar.js:362-367` | `applyVisibility` uses `Array.find` inside SVG loop = O(n²). Add `Map<connId, connection>` for O(1). |
| P7 | **Targeted `renderSelectionState`** | `renderer.js:290-294` | Currently toggles `.selected` class on all 130 nodes. With DOM cache, target only changed nodes. |

### Phase 3 — Architecture Refactoring (Priority: HIGH, ~4-6h)

| # | Task | File(s) | Details |
|---|------|---------|---------|
| A1 | **Split sidebar.js into domain modules** | `sidebar.js` (730 lines) | Extract: `project-manager.js` (save/load/delete/import/download), `settings-manager.js` (theme/grid/connection style), keep `sidebar-ui.js` as thin rendering layer. |
| A2 | **Unify duplicate `loadProject` logic** | `project.js:11`, `sidebar.js:628-655` | Two different `loadProject` functions with divergent behavior (one calls `renderLegend`, other resets groups/undoStack). Merge into single source of truth. |
| A3 | **Introduce lightweight event bus** | `state.js` (new pattern) | Replace `fullRenderFn` callback threading in 4 modules (`interactions.js:27`, `sidebar.js:12`, `keyboard.js:16`, `toolbar.js:16`) with `state.on('change', ...)` subscriptions. |
| A4 | **Extract snapshot helper in state.js** | `state.js:53-93` | Snapshot creation logic duplicated 3x (saveSnapshot, undo, redo). Extract `createSnapshot()` function. |
| A5 | **Extract bounding box helper** | `export-png.js:17`, `transform.js:35`, `minimap.js:25` | Same bounding box calculation in 3 files. Move to `utils.js` as `getNodesBoundingBox()`. |
| A6 | **Extract project serialization helper** | `sidebar.js:597-608`, `sidebar.js:686-697` | Project data shape built manually in 2 places. Create single `serializeProject()`. |
| A7 | **Use consistent ID generation** | `sidebar.js:293` | Group IDs use `Date.now()` while rest of app uses `genId()`. Use `genId()` everywhere. |

### Phase 4 — Code Quality & Error Handling (~2h)

| # | Task | File(s) | Details |
|---|------|---------|---------|
| Q1 | **Fix silent catch blocks** | `sidebar.js:591,615,624,672`, `persistence.js:28`, `build.js:31,60,85` | Add `console.warn` or `showToast` — currently swallows errors silently. |
| Q2 | **Add null checks on critical DOM lookups** | `main.js:130`, `toolbar.js:50-91` | `getElementById` results used without null checks. One missing element crashes entire editor. |
| Q3 | **Fix two-frame render split** | `main.js:27-36` | `renderNodes()` runs sync, but `renderConnections()` deferred to next rAF — causes 1-frame flicker where nodes show without connections. |
| Q4 | **Add legend cleanup on project switch** | `project.js:52-63` | `renderLegend()` creates DOM but has no cleanup when loading a different project or creating new. |
| Q5 | **Standardize naming conventions** | `interactions.js:18-20` | Inconsistent underscore prefix (`_popupJustOpened` vs `isDragging`). Pick one convention. |
| Q6 | **Move inline styles to CSS** | `sidebar.js:380+` | HTML templates contain inline `style="font-size:8px;padding:1px 4px;..."` bypassing token system. |
| Q7 | **Move `:root` vars to tokens.css** | `sidebar.css:1-3` | `--sidebar-w` defined in `sidebar.css` instead of `tokens.css` with all other tokens. |

### Phase 5 — Scalability (for 500-node PRD target, ~6-8h)

| # | Task | File(s) | Details |
|---|------|---------|---------|
| SC1 | **Incremental node rendering** | `renderer.js:25-29` | Replace `innerHTML = ''` teardown with in-place DOM updates using the element cache Map. Only create/remove/update changed nodes. |
| SC2 | **Incremental connection rendering** | `renderer.js:149-150` | During drag, update only SVG attributes of connections attached to moving nodes instead of full SVG rebuild. |
| SC3 | **Viewport culling** | `renderer.js` | Only render nodes visible in current pan/zoom window. Skip off-screen nodes entirely. |
| SC4 | **Command-based undo/redo** | `state.js` | Replace full-state JSON snapshots (80 × ~50KB = 4MB) with discrete commands (AddNode, MoveNode, etc.) that know how to apply/reverse. |
| SC5 | **CSS minification in build** | `build.js:22-34` | CSS concatenated but not minified. Add esbuild CSS minification for ~10-20KB savings. |
| SC6 | **Add source maps to build** | `build.js` | No source maps generated — debugging production build is difficult. |

### Phase 6 — Feature Tasks (from original plan)

| # | Task | Status | Details |
|---|------|--------|---------|
| F1 | **Search/filter nodes in sidebar** | Not started | P0 per PRD — needs event bus (A3) for "dim non-matching" |
| F2 | **Group management UX** | Not started | Drag-to-assign nodes to groups, reorder groups |
| F3 | **Connection endpoint editing** | Partially wired | Drag endpoints to reconnect (renderer/interactions have some support) |
| F4 | **Export with visibility** | Not started | Export only visible nodes (filtered by group toggles) |
| F5 | **Legend integration** | Not started | Sync legend with sidebar groups/sectors |
| F6 | **No-data-loss project loading** | Not started | Confirmation dialog before loading new project overwrites current |

### Nice-to-Have
- Sidebar width resizable via drag handle
- Keyboard shortcut help overlay
- Multi-language support (currently German labels hardcoded)
- Node type registry/plugin system for extensibility
- Touch support via Pointer Events (PRD P1)

---

## Context Loading for Next Session

```
Files to read first (~3K tokens):
- src/js/sidebar.js        — main sidebar logic
- src/js/state.js           — state shape including groups/hiddenSectors
- src/js/main.js            — init sequence and fullRender
- src/styles/sidebar.css    — sidebar layout/styling
- build.js                  — build pipeline with embedded projects

For deeper context (~5K tokens):
- src/js/renderer.js        — node/connection rendering
- src/js/interactions.js    — drag/connect/selection logic
- src/js/persistence.js     — localStorage save/load
- src/index.html            — full HTML structure
```
